name: linux-daily
on:
  schedule:
    - cron: "0 4 * * *"
    - cron: "0 5 * * *"
  workflow_dispatch:
jobs:
  build:
    name: Linux build
    runs-on: ubuntu-latest
    steps:
      - name: Check out source code
        uses: actions/checkout@v6

      - name: Install bun
        uses: oven-sh/setup-bun@v2

      - name: Check Warsaw schedule time
        id: schedule
        shell: bash
        env:
          TZ: Europe/Warsaw
        run: |
          if [[ "${{ github.event_name }}" != "schedule" ]]; then
            echo "run_build=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "Warsaw local time: $(date)"
          if [[ "$(date +%H)" == "06" ]]; then
            echo "run_build=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_build=false" >> "$GITHUB_OUTPUT"
          fi

      # The Linux build links the GTK 4 keyboard-help viewer and SumatraPDF app,
      # so gtk4.pc & friends must be present; ubuntu-install-deps.sh is the
      # single place where those package names live.
      - name: Install build dependencies
        if: steps.schedule.outputs.run_build == 'true'
        run: sudo sh cmd/ubuntu-install-deps.sh

      - name: Build and run test_util
        if: steps.schedule.outputs.run_build == 'true'
        run: bun cmd/build.ts -linux -asan
