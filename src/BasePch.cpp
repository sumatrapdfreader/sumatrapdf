// This file exists only to generate the precompiled header for base/Base.h
// (configured via pchheader/pchsource in premake5.lua). Base.h is included
// first by almost every .cpp, and parsing it is ~10% of compile time, so
// precompiling it speeds up clean builds.
#include "base/Base.h"
