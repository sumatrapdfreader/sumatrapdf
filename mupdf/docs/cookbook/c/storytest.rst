.. highlight:: c

Stories
==========

The ``storytest.c`` example uses the Story API to create a document
using an HTML template and save it as a PDF.

.. literalinclude:: ../../examples/storytest.c
