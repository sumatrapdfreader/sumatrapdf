# Update check doesn't work?

A new version of SumatraPDF has been released, but the `Help → Check for Updates` menu didn't notify you. Is this a bug?

Most likely not.

We wait at least a week before activating the update check. That way, if there is a major problem with a new release that we didn't catch in testing, fewer people will have to download another update containing the fix.
